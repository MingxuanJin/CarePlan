services:
  web:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    environment:
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-2.0-flash}
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
